// DOM Ready =============================================================
$(document).ready(function () {
    /*=============================================================*/

    /******* Contact View************/

    // Populate the user table on initial page load
    getContactList();

    // Add Contact event 
    addContactEvent();

    //setTimeout(loadreThink, 3000);


});